용어

지인(게스트): 사용자의 면접 영상을 보고 피드백을 남기는 사람.

MVP에서 서비스에 가입하지 않고 링크로만 접근한다.

평가 항목(태도 5종):

지인이 채우는 5가지 축 - 시선 / 표정 / 자세 / 손 움직임 / 목소리 크기.

AI가 판단하지 않기로 한(Part 3 §2-2·§5) 비언어·태도 영역.

MVP 기준으로 AI 축과 겹치지 않고 보완한다(반언어 통계가 도입되면 §6에서 중복을 재검토한다).

4단계 척도: 각 항목을 4단계로 고르는 선택지.

왼쪽이 긍정("좋았어요"), 오른쪽이 아쉬움("아쉬웠어요")

Part 2 내부 루브릭의 "4점 척도(1~4점)"와 다른 것이므로 "4점"이라는 표기는 쓰지 않는다.

예) 시선

잘 맞춰요 / 대체로 맞춰요 / 가끔 피해요 / 자주 피해요.

피드백 세션: 지인 한 명의 평가 1회.

지정 항목을 모두 채우고 제출하면 1세션이 완료된다.

별칭: 지인이 스스로 입력하는 표시 이름.

최종 레포트에서 지인 카드를 구분하는 라벨.

미입력 시 지인1~4를 자동 부여한다.

공유 링크(토큰): 사용자가 생성해 지인에게 전달하는 접근 주소.

최초 링크 생성이 곧 "피드백 요청" 사건이다(§5 Step2, 홈 PRD 시각 4종과 동일 사건).

대본: 답변을 받아쓴 글(STT 텍스트).

Part 3와 같은 말이며 트랜스크립트라는 표기는 쓰지 않는다.

하이라이트 구간: 채점 근거가 된 대본 문장 구간(시각 구간 참조 + 잘함/개선 구분).

Part 2 변경 요구서 B-1의 표준 명칭.

질문 경계: 각 턴의 시작 시각(Part 3 플레이어 규칙).

챕터라는 말은 폐기된 구버전(V0) 용어라 쓰지 않는다.

지인 피드백 섹션: R1 레포트 하단에 붙는 영역. 태도분석 탭 + 기타 탭(표시명 "하고싶은 말", 디자인 확정 대기)으로 구성한다.

최종 레포트: R1(AI 축)에 지인 피드백 섹션(지인 축)이 쌓인 상태. 지인 1명 제출 시 성립하고 도착 즉시 갱신되며 확정 시점이 없다(홈 PRD 정의와 동일). 별도 화면 코드를 두지 않고 R1을 확장한다. 표기는 홈·마이페이지·이용권 PRD와 같이 "레포트"로 쓴다(Part 3의 "리포트" 표기와의 문서군 통일은 별도 결정).

화면 코드: R1 = 1차 레포트 / 최종 레포트(Part 3와 동일). F4 = 지인 공유 설정 화면(사용자측, 구 S4에서 개명). G4 = 지인 평가 화면(게스트측).

0. 이 단계가 푸는 문제 & 방향성

무엇을 푸는가

AI가 원리적으로 보지 못하는 것(눈빛·표정·자세 같은 비언어 태도)을, 사용자를 아는 사람이 짧은 시간 안에 채워 넣게 한다. 그 결과를 AI 축과 나란히 놓아 "내용(AI) + 태도(지인)"의 최종 레포트를 만든다.

방향성

AI와 지인의 역할을 겹치지 않게 나눈다.

AI는 답변의 내용·역량(Part 3)을, 지인은 태도 5종을 본다.

지인에게는 영상과 질문 경계만 넘긴다.

AI 피드백(하이라이트·행동형 키워드·고쳐쓰기·한 줄 요약·질문 분석·레드플래그)은 넘기지 않는다.

지인이 AI 판정에 이끌려 평가가 오염되는 것을 막는다.

지인의 진입 장벽을 없앤다.

가입 없이 링크만으로 10분 안에 평가를 끝낼 수 있게 한다.

지인 피드백은 AI 분석과 독립이다.

분석 부족 면접이어도 영상만 있으면 지인 태도 피드백은 유효하다.

사용자가 노출을 통제한다.

공유한 뒤에도 언제든 비공개로 돌려 새 접근을 끊을 수 있다.

원본 영상은 단계별로만 연장 보관하고 상한(최대 30일) 안에서 자동 삭제한다.

한 명이라도 피드백을 제출하면 제출한 내용을 최종 레포트에서 노출한다.

이후 추가로 3명이 제출하면 최종 레포트에서 볼 수 있는 카드가 추가된다.

정책 요약

영역

내용

진입점

R1 레포트의 [지인에게 면접 영상 보내기] 버튼 → F4

지인이 받는 것

면접 영상

그리고 사용자가 지정한 평가 항목

지인이 못 받는 것

R1에서 사용자가 볼 수 있었던 구성 요소들

AI 하이라이트

행동형 키워드

고쳐쓰기 제안

한 줄 요약

질문 분석

레드플래그

지인 인원

최소 1명이라도 피드백 작성 시, 최종 레포트에서 지인 섹션을 노출한다.

면접당 최대 4명 누적으로 피드백 작성할 수 있다.

각 지인은 본인이 작성한 별칭으로 구분한다. 카운트는 참고치(§2-5)

제출 필수 조건

사용자가 지정한 항목을 모두 4단계 척도에 맞춰서 제출 완료한 상태(코멘트·전반 피드백은 선택)

제출 이후 수정 불가(§2-3)

노출 방식

R1 하단 지인 피드백 섹션.

항목별 그룹 아래 지인별 개별 카드(태도분석)

지인별 전반 피드백(기타)

집계·다수결 표기는 하지 않는다(§2-4)

보상

지인 몇 명이 제출하든 사용자에게 보상을 제공하지 않는다

MVP에서는 보상 미제공 확정

마감 기한

별도 마감을 두지 않는다.

영상 만료(§5)가 사실상의 마감이다

영상 보관

단계별로 기간을 연장하며, 중복일 경우 마감기간이 더 긴 쪽을 적용한다

최대 30일까지 마감될 수 있다.

레포트·대본·하이라이트 구간·지인 응답은 영구(§5)

통제권

사용자가 공개/비공개 전환.

비공개 = 그 링크의 되돌릴 수 없는 종료(재공개 없음, 새 링크 생성은 가능)

기존 제출 피드백은 유지(§2-6)

1. 전체 화면 흐름

[R1 레포트 (Part 3에서 도달)]
   │  [지인에게 면접 영상 보내기]
   ▼
F4. 지인 공유 설정
   ├─ (1) 평가 항목 지정 (태도 5종 중 1~5개)
   ├─ (2) 링크 생성 → 외부 공유(메신저 등)
   │      ※ 생성 후 항목 잠금. 최초 생성 = "피드백 요청"(§5 Step2, +48h)
   └─ (3) 공유 관리: 공개/비공개 토글(되돌릴 수 없음), 참여 현황
        │
        │        ┌───────────── 지인측 (가입 없음) ─────────────┐
        └─링크──▶ G4. 지인 평가
                   ├─ 온보딩(요청 안내 + 별칭 입력 + "AI가 못 보는 것" 안내)
                   ├─ 영상 시청(상단 고정) + 항목별 바텀시트 평가
                   ├─ 전반 피드백 텍스트(선택)
                   └─ 제출 (지정 항목 전부 평가해야 활성)
                          │
                          ▼
              [지인 1명 제출 완료 = 최종 레포트 성립 (§5 Step4, +30일)]
                          │
                          ▼

R1 하단 '지인 피드백' 섹션 활성
   ├─ 태도분석 탭: 항목별 그룹 → 지인별 개별 카드 (최대 4명)
   └─ 기타 탭: 지인별 전반 피드백



중도 이탈로 Part 2가 세션을 초기화한 경우(STT 30% 초과 실패 포함, Part 2 §3.5·§9)는 R1에 도달하지 않으므로 이 단계에 들어오지 않는다. 8분 이후 종료 버튼으로 일찍 끝낸 조기 종료 면접은 R1에 도달하므로 공유할 수 있다.

분석 부족 면접도 R1에 도달하므로 지인 공유가 가능하다. 지인 축은 AI 분석 성패와 무관하게 성립한다.

2. 핵심 로직

2-1. 지인에게 넘기는 것 / 넘기지 않는 것

대상

지인에게

제공 근거

면접 영상(원본)

✅ 재생

태도는 영상 내 목소리으로만 AI가 판단할 수 없지만, 사람은 영상을 보고 판단할 수 있다

질문 경계(타임라인)

✅

어떤 질문 구간인지 맥락을 제공하기 위해서이다.

사용자 이름/별명

✅

사용자와 지인은 서로 아는 사이를 전제로 한다

지정 평가 항목

✅ 지정한 것만

§2-2

AI 하이라이트·행동형 키워드·고쳐쓰기

❌

사람이 피드백할 때 보여주게 되면 평가에 오염이 생길 수 있다. 이를 방지한다.

한 줄 요약·질문 분석·레드플래그·내부 점수/판정

❌

상동. 질문 분석(질문 의도 번역, Part 2 변경 요구서 B-5)도 AI 피드백이므로 넘기지 않는다

2-2. 평가 항목 지정 규칙 (F4)

사용자는 태도 5종 중 최소 1개, 최대 5개를 고른다.

기본값은 5개 전체 선택된 상태로 제공하고, 사용자가 본인이 피드백 받고 싶지 않은 측면을 덜어낼 수 있게 한다.

지정 항목이 0개라면 피드백을 요청하는 링크를 생성할 수 없다.

지인 제출 조건이 "지정 항목 전부 평가"라 최소 1개가 있어야 성립한다.

지정하지 않은 항목은 G4에서 지인에게 노출하지 않는다.

링크 생성 후 사용자가 피드백을 요청한 항목은 lock 상태로 변경되어 피드백을 요청한 링크에 귀속된다.

링크 생성을 클릭하면 이전에 생성해두었던 링크를 다시 복사할 수 있다.

피드백을 요청한 항목을 바꾸려면 아예 새로운 피드백 요청 링크를 생성해야 한다. 이전 링크는 자동 무효화된다(§2-5).  → 2026년 7월 18일 팀 전체 논의 후 MVP에서 해당 기능은 제거하기로 함

2-3. 제출 성립 규칙 (G4)

지인은 지정된 피드백 항목은 하나도 빠짐없이 4단계 척도로 골라야 [제출]이 활성화된다.

최소 1개 최대 5개이다.

항목별 코멘트("왜 그렇게 느꼈나요?")와 전반적인 면접에 대한 피드백(기타)은 선택 입력사항이다.

항목별 코멘트: 국문, 영문, 공백, 숫자, 특수문자 ! - ~ ? . , / [ ] < >를 포함해서 100자까지 입력 가능

항목별 코멘트 입력 시, 키패드가 위로 올라올 때 input field 높이를 고정해야 하므로, 입력된 텍스트는 최대 3줄까지 노출하고 그 이상인 4줄 째부터 스크롤 바 노출한다.

전반적인 면접에 대한 피드백 코멘트: 국문, 영문, 공백, 숫자, 특수문자 ! - ~ ? . , / [ ] < >를 포함해서 300자까지 입력 가능

제출된 세션 1건 = 지인 1명

R1 지인 피드백이 하나라도 제출되면 즉시 최종 레포트가 생성된다.

기존 영상 보관이 30일로 연장된다(§5 Step4).

제출 = 제출한 뒤에는 동일 링크로 접근한 지인이 내용을 수정할 수 없다.

실수를 막기 위해 제출 직전 확인 다이얼로그("제출하면 다시 고칠 수 없어요")를 1회 띄운다.

동일 링크에 대해서 동일 지인이 재접속 시 처리는 §2-5를 따른다.

2-4. 집계·노출 규칙

원칙

지인별 피드백은 개별 카드로 나열한다.

같은 항목을 지인들이 다르게 평가해도 하나로 합치거나 통계를 보여주지 않는다.

별칭 라벨로 각자 표시한다.

지인마다 시선이 다른 것 자체가 사용자에게 유의미한 정보이기 때문이다.

디자인 변경 필요

구버전 목업과 2026-07-11 디자인 시안의 "항목당 값 하나 + 지인 N명 칩" 집계 표기는 이 정책으로 대체한다

디자인 수정 요청은 §7에 정리해둠

구성 항목

태도분석 탭: 항목(시선·표정·…)별로 묶고, 그 아래 지인별 카드(별칭 · 척도값 · 코멘트)를 나열한다.

기타 탭: 지인별 전반 피드백 원문을 카드로 나열한다.

지인마다 평가해야하는 항목이 다를 수 있다.

만약 사용자가 평가받고 싶은 항목을 바꿔 새 링크를 만들어 전달한다면, (§2-2·§2-5) 링크 시점에 따라 지인별 평가 항목 집합이 갈린다.

시스템이 만드는 라벨·안내 문구는 중립으로 쓴다. 지인이 직접 쓴 코멘트 원문은 그대로 노출한다.

지인 평가 상태 규칙

상황

처리

항목을 아무도 평가하지 않음

그 항목 그룹 자체를 노출하지 않는다(빈 슬롯·결측 표기 없음)

일부 지인만 평가한 항목

평가한 지인의 카드만 노출한다("N명 중 M명" 표기 없음)

코멘트 없는 카드

별칭 + 척도값만 보여준다

3·4번째 지인의 늦은 제출

해당 항목들에 카드가 추가된다(섹션 재생성 없음, 실시간 갱신)

지인 0명

섹션 대신 "아직 받은 지인 피드백이 없어요" + [지인에게 보내기] 유도

참여 인원을 표기할 때는 항목별 칩이 아니라 섹션 상단에 한 번, 단정하지 않는 톤으로 쓴다

예: "지금까지 2명이 참여했어요"

사유: 인원은 상한선이 있고, 사용자가 참고할 수 있도록 안내되어야 하기 때문이다(§2-5에서 추가 서술).

2-5. 인원·중복·링크 재생성 처리

인원 상한 (면접별 누적)

상한은 링크별이 아니라 면접별이다. 한 면접에 대해 누적 최대 4명까지 제출을 접수한다.

평가 받고 싶은 항목을 변경하여 새 피드백 요청 링크를 만들어도 슬롯은 리셋되지 않는다.

이미 2명이 제출했다면 새 링크로는 남은 2슬롯만 채운다.

4명이 차면 이후 방문자는 (공개 상태라면) 영상은 볼 수 있으나 제출 버튼은 비활성 상태로 변경한다.

"이미 4분이 참여했어요" 안내를 화면에 제공한다.

4명의 정원이 다 차면, 항목 변경을 할 수 없도록 제한한다.

링크 재생성 = 이전 링크 무효화

→ 2026년 7월 18일 팀 전체 논의 후 MVP에서 해당 기능은 제거하기로 함

한 면접당 활성 링크는 항상 1개다.

이미 생성된 링크가 있을 경우, 링크 생성을 클릭하면 새로운 링크가 만들어지는 것이 아니라 기존 링크가 복사된다.

토스트 메시지로 “피드백 요청 링크를 복사했어요.” 등 코멘트를 노출한다.

새로운 피드백 요청 링크를 생성하면 이전 링크는 즉시 무효화된다.

무효화된 링크로 들어오려는 지인, 그리고 이전 링크로 평가 중이던(미제출) 지인의 작성분은 폐기된다

"지금은 볼 수 없는 영상" 안내

§2-6의 비공개 처리와 동일하다.

이미 지인이 제출 완료한 피드백은 무효화 링크와 무관하게 최종 레포트 상에서 유지된다.

"피드백 요청" 시각(§5 Step2, 홈 PRD 시각 4종)은 최초 링크 생성 시각으로 고정한다.

재생성해도 갱신하지 않는다. Step2 연장은 최초 1회만 의미가 있기 때문이다.

제출 후·재접속 처리

제출은 확정이다. 같은 브라우저로 다시 들어와도 수정할 수 없다.

"이미 제출하셨어요. 소중한 피드백 고마워요" 안내만 보여준다.

"다음에 하기"로 나갔다가(미제출) 다시 들어오면 임시저장분을 복원한다(§3 G4 상태표).

이것은 제출 전 초안을 되살리는 것이라, 제출 후 수정을 막는 위 정책과 부딪히지 않는다.

지인 한 명 한 명은 브라우저(로컬 토큰)로 구분한다.

로컬 토큰의 역할은 같은 브라우저의 중복 제출을 막는 것 하나뿐이다.

카운트는 참고치다

게스트는 계정이 없어서, 시크릿 모드·다른 기기·쿠키 삭제를 쓰면 같은 사람인지 가려낼 수 없다.

그래서 지인 수는 정확한 값이 아니라 참고치로 다루고, 이를 더 강하게 막는 장치도 두지 않는다(강화 여부는 §6).

사용자에게 보여줄 때도 인원을 단정하지 않는 톤으로 쓴다.

예: "지금까지 2명이 참여했어요"

참고치 위에는 보관·게이트처럼 정확히 서버가 계산해야 하는 정책을 얹지 않는다.

§5 Step4의 트리거를 "몇 명이 참여했나"가 아니라 "제출이 1건이라도 있나"로 정한 이유다.

제출이 있었는지의 판정은 우회와 무관하게 견고하다.

2-6. 공개/비공개 전환

비공개로 바꾸면 공유 링크가 즉시 차단된다.

그 뒤 링크로 들어오는 지인에게는 "지금은 볼 수 없는 영상이에요" 안내를 보여준다.

이미 제출된 피드백은 R1에 그대로 남는다.

비공개는 새로운 접근을 끊을 뿐, 이미 받은 피드백을 지우지 않는다.

다만 평가하던(미제출) 지인의 작성분은 폐기된다.

비공개는 그 링크의 되돌릴 수 없는 종료다.

같은 링크를 다시 여는 재공개 기능은 없다.

재공개를 두지 않는 이유:

재공개를 만들면 토큰 수명·재발급·기존 링크 무효 처리를 통째로 설계해야 한다.

재공개를 하는 시나리오 자체가 MVP 시점에는 드물다고 판단했다.

새로 공유하는 쪽이 더 단순하고, 밖으로 새어 나간 링크를 끊는 데도 유리하다.

다시 피드백을 받고 싶으면 새 링크를 만들어 새로 공유한다(§2-5).

즉 끝나는 것은 그 링크 하나이지, 그 면접의 공유 기능 전체가 아니다.

비공개로 바꿔도 영상 보관 시점의 계산은 달라지지 않는다.

접근만 끊길 뿐, 이미 계산된 삭제 예정 시점은 그대로 흘러간다(§5).

실수를 막기 위해 전환 직전에 경고 다이얼로그("이 링크는 다시 열 수 없어요")를 1회 띄운다.

3. 화면별 상세

F4. 지인 공유 설정 (사용자측)

목적:

사용자가 R1에서 넘어와 지인에게 평가받을 항목을 고르고 링크를 만든다.

공유한 뒤에는 이 화면에서 공개 상태와 참여 현황을 관리한다.

구성요소

평가 항목 지정: 태도 5종을 토글로 고른다.

기본은 5개 전체 선택이고, 최소 1개는 남겨야 한다(§2-2).

 링크를 만들고 나면 항목은 잠긴다.

링크 생성과 공유: [링크 생성]을 누르면 공유 시트가 열리고, 만들어진 링크를 메신저 등으로 내보낸다.

최초 링크 생성이 곧 "피드백 요청" 사건이 된다(§5 Step2).

항목을 바꾸고 싶으면 새 링크를 만들어야 하며, 이때 이전 링크가 무효화된다는 안내를 함께 보여준다(§2-5).

공유 관리: 세 가지를 보여준다.

공개/비공개 토글. 비공개로 바꾸기 직전에 되돌릴 수 없다는 경고 다이얼로그를 1회 띄운다(§2-6).

참여 현황

요청함(= 최초 링크를 만들었음)

지인이 영상을 봤는지, 제출한 인원을 보여준다.

인원은 참고치라 "지금까지 2명이 참여했어요" 같은 톤으로 쓴다(§2-5).

영상 만료 예정. §5에서 계산된 삭제 예정 시점을 보여준다.

G4. 지인 평가 (게스트측)

a) 온보딩

첫 화면에서 누가 왜 불렀는지를 알려준다:

"OO님이 당신께 피드백을 요청했어요 / 이동 시간 중 딱 10분만 빌려줄 수 있나요?"

그 아래 안내 두 줄로 지인의 역할을 잡아준다.

"AI가 못 보는 것도 있어요 - 눈빛·말투 같은 순간은 OO님만 알아요."

"정답을 채점하는 자리가 아니에요 - 화면 속 모습(눈빛·표정·자세·손·목소리)이 평소처럼 자연스러웠는지만 봐주세요."

별칭을 입력받는다. 선택 입력이고, 비워두면 지인1~4를 자동으로 붙인다.

이 값이 최종 레포트에서 카드를 구분하는 라벨이 된다.

하단 버튼은 [피드백 시작하기]와 "다음에 하기" 두 개다.

b) 영상 + 바텀시트 평가

평가가 시작되면 영상이 화면 상단에 고정되고, 그 아래에 평가 항목 탭(시선·표정·…)이 나타난다.

항목을 고르면 바텀시트가 올라온다. 시트 안은 위에서부터 이렇게 구성한다.

질문형 헤드라인. 예: "OO님은 눈을 잘 마주치나요?"

4단계 척도. 왼쪽이 긍정("좋았어요"), 오른쪽이 아쉬움("아쉬웠어요")이다.

"왜 그렇게 느꼈나요?" 텍스트 입력. 선택이다.

[영상 크게 보기] / [저장] 버튼.

지인은 지정된 항목을 하나씩 돌며 같은 방식으로 평가한다.

c) 전반 피드백(기타): 항목과 별개로 자유롭게 쓰는 텍스트다. 선택사항이다.

d) 제출: 지정 항목을 모두 채우면 [제출]이 켜진다(§2-3).

제출을 누르면 "제출하면 다시 고칠 수 없어요" 확인 다이얼로그를 1회 띄운다.

지인이 확인을 클릭하면 제출이 확정되며 완료 안내를 보여준다.

상태

상태

처리

정상

온보딩 → 평가 → 제출

이어하기(미제출)

"다음에 하기" 후 재진입 시 임시저장분 복원.

영상 만료 전까지만 이어하기가 가능하다. 평가 도중 만료되면 작성분은 유실된다(감수하는 리스크, §5·§6)

제출 완료 후 재접속

수정 불가 - "이미 제출하셨어요" 안내(§2-5)

정원 마감(면접당 4명)

영상 시청만 가능, 제출 비활성 + "이미 4분이 참여했어요" 안내

비공개/링크 무효화됨

링크 차단 화면 + 미제출 작성분 폐기(§2-5·§2-6)

영상 만료

시청·평가 불가 안내(§5)

R1 (최종 레포트 = 지인 피드백 섹션 확장)

지인이 1명이라도 제출하면 R1 하단에 지인 피드백 섹션이 나타난다.

상단의 AI 상세 레포트는 Part 3 그대로 유지된다.

섹션은 태도분석 탭과 기타 탭으로 나뉜다.

기타 탭의 표시명("하고싶은 말")은 디자인 확정을 기다린다.

태도분석 탭은 항목별로 묶고, 그 아래 지인별 카드(별칭 · 척도값 · 코멘트)를 나열한다.

어떤 항목과 카드를 보여주고 숨기는지는 §2-4의 상태 규칙 표를 따른다.

기타 탭은 지인별 전반 피드백을 카드로 나열한다.

최종 레포트가 성립한 뒤 3·4번째 지인이 뒤늦게 제출하면, 섹션을 다시 만들지 않고 카드만 실시간으로 추가한다.

홈 기록 위젯이 "최종"으로 바뀌고 마지막 업데이트 시각이 갱신되는 규칙은 홈 PRD 3.3을 따른다.

지인이 아직 0명이면 섹션 자리에 "아직 받은 지인 피드백이 없어요" 안내와 [지인에게 보내기] 버튼을 보여준다.

4. 데이터 파이프라인

원천 데이터

최초 생성 시점

DB 저장 형식

보관 정책

R1/지인 화면

공유 링크(토큰)

링크 생성 시

token + 대상 면접 + 지정 항목 + 상태(활성/비공개) + 생성 시각

면접당 활성 1개, 비공개와 활성 공존 불가.

최초 토큰의 생성 시각이 "피드백 요청" 시각(§5 Step2·홈 PRD 시각 4종)

지인 진입 게이트

지정 평가 항목

F4에서 지정

항목 집합(1~5), 토큰에 귀속

영구. 링크 생성 후 잠금

G4 노출 항목 결정

지인 척도 응답

지인 제출 시

항목별 4단계 척도값 + 별칭 + 세션ID

영구. 제출 시 확정(수정 불가)

태도분석 카드

지인 코멘트/전반 피드백

지인 제출 시

텍스트 원문 + 별칭

영구

태도분석/기타 카드(원문)

공개/비공개 상태

F4 토글 시

토큰 상태로 일원화 제안

🔴 서버 협의 필요: 별도 boolean을 둔다면 게이트 판정 원천을 명시

영구(행동 로그)

링크 게이트

영상(원본)

면접 중 녹화

영상 파일 + timestamp

단계별 연장, 최대 30일(§5)

G4 시청 / R1 다시보기

레포트·대본·하이라이트 구간(근거 시각 포함)·지인 응답은 영구 보관하고, 영상 원본만 삭제 사이클을 탄다(Part 2 §10·Part 3 §4와 정합).

5. 영상 보관 사이클

무엇에 적용되나

이 사이클은 영상 원본에만 적용된다. 레포트·대본·하이라이트 구간·지인 응답은 영구 보관이다(§4).

기본 원칙: 영상은 24시간만 보관하되, 지인 피드백이 진행될수록 보관이 길어진다.

아래 표의 사건(Trigger)이 일어날 때마다 삭제 예정 시점을 다시 계산한다.

계산 규칙 세 가지

기준 시각은 하나다. 모든 단계의 삭제 예정 시점은 "1차 레포트 생성 성공 시각"에서 출발해 계산한다.

이용권 PRD가 차감 기준으로 쓰는 사건과 같은 사건이다.

항상 더 긴 쪽을 적용한다. 새로 계산한 시점이 기존 시점보다 이르면 기존 시점을 그대로 둔다.

본인이 자기 영상을 다시 보는 것은 연장 사유가 아니다.

단계

연장 사건 (Trigger)

삭제 예정 시점

예) 사건 시각

예) 삭제 예정

Step1

1차 레포트 생성 성공

생성 시점 + 24h

6/1 13:00 생성

6/2 13:00

Step2

최초 공유 링크 생성(= 피드백 요청)

생성 시점 + 48h

6/1 14:00 링크 생성

6/3 13:00

Step3

지인 최초 영상 조회

생성 시점 + 7일(168h)

6/3 11:00 최초 조회

6/8 13:00

Step4

지인 1명 피드백 제출(= 최종 레포트 성립)

생성 시점 + 30일(720h)

6/4 15:00 첫 제출

7/1 13:00

Step4를 "1명 제출"로 정한 이유

구버전의 "지인 2명" 기준은 폐기된 2명 보상의 잔재다.

지인 수는 참고치라서(§2-5) 그 위에 보관 정책을 얹으면, 같은 사람이 두 번 제출해도 연장되는 모순이 생긴다.

반면 "제출이 1건이라도 있는가"의 판정은 우회와 무관하게 견고하다.

만료 시 처리

지인 쪽(G4): 시청·평가 버튼이 꺼지고 아래 공통 문구를 보여준다.

사용자 쪽(R1): [영상 다시보기]·[이 장면 영상으로 보기]가 꺼지고 같은 문구를 보여준다. 지인 피드백 섹션(척도·텍스트)은 영상이 지워져도 그대로 남는다.

만료 안내 문구

Part 3의 "24시간이 지나서…" 고정 문구를 대체한다.

공통으로 항상 보여준다: "보관 기간이 지나 영상이 삭제되었어요."

그 면접에 공유 링크를 만든 적이 없을 때만 한 줄을 덧붙인다:

"다음 면접 연습 때는 지인에게 피드백을 요청해 보세요. 영상을 더 오래 볼 수 있어요."

분기 기준을 단계가 아니라 "링크를 만든 적이 있는가"로 두는 이유: 서버가 이미 아는 값 하나로 판정할 수 있고, 이미 공유해 본 사용자에게 사실과 다른 유도를 하지 않게 된다.

🔴 서버 협의 대기 (착수 전 blocker)

위 표의 단계 전이는 "직전 단계의 영상이 아직 살아 있을 때만 다음 단계로 넘어갈 수 있다"는 전제 위에 있다.

확인할 것: 영상이 이미 삭제된 뒤에 링크 생성(Step2)이나 조회(Step3)가 들어오면 연장이 불가능한가?

불가로 확정되면, 만료 후 요청·조회를 시도할 때 차단 문구("영상이 만료되어 공유할 수 없어요")를 F4·G4에 추가한다.

영상 보관 타이머를 보류하는(hold) 정책은 미제공 (마이페이지 PRD 파킹 회수, 폐기 확정)

영상 보관은 사건이 일어난 시점에 만료 시점을 다시 계산하는 방식이다.

지인이 평가하는 중이라는 이유로 만료를 잠시 멈추는 보류 규칙은 만들지 않는다.

만들 수 없는 이유: 게스트의 임시저장이 클라에만 있어서, 서버는 지금 누가 평가 중인지 알 수 없다(저장 위치는 🔴 서버 협의 3에서 확인).

그래서 만료 직전까지 평가하던 지인의 작성분이 사라지는 것은 감수한다.

이를 막는 보호 장치는 §6에서 재검토한다.

비공개 전환도 만료 시점을 바꾸지 않는다(§2-6).

만료 임박 알림

"곧 삭제되니 지금 지인에게 보내라"는 푸시 알림은 과설계 소지가 있어 MVP에서 제외한다.

운영 데이터를 본 뒤 §6에서 재검토한다.

6. MVP 이후 결정할 내용

#

항목

MVP 현재 처리

나중에 무엇을 보고 정하나

선행 조건·의존

1

지인 카드 통제(신고·숨김·삭제)

원치 않는 지인 피드백도 숨기거나 지울 수 없다(감수하는 리스크)

운영 중 부적절하거나 원치 않는 피드백 관련 문의가 얼마나 나오나

없음(운영 데이터)

2

재공개

비공개 = 그 링크의 되돌릴 수 없는 종료로 확정(§2-6)

재공개를 원하는 사례가 데이터로 확인되나

토큰 재발급·기존 링크 무효 처리 설계

3

지인 1인 판별 강화

로컬 토큰 기반 소프트 제한, 카운트는 참고치(§2-5)

중복·우회 제출이 실제로 문제가 되나

개별 링크 발급·간이 인증(#10과 한 갈래)

4

만료 임박 평가 중 지인 보호

만료 직전 평가 세션의 작성분 유실은 감수(§5)

유실이 실제로 얼마나 발생하나

게스트 임시저장이 브라우저 → 서버 저장으로 전환(🔴 서버 협의 3)

5

만료 임박 유도 알림(푸시)

도입하지 않음(과설계 소지로 제외, §5)

만료 전에 공유하지 않은 비율이 높나

푸시 알림 기획·인프라

6

집계 뷰(항목별 요약·분포)

개별 카드만 노출(§2-4)

지인 수가 늘어 개별 카드 나열이 길어지나

개별 카드 원칙이 기본, 집계는 보조 뷰로만

7

리마인드

재알림 없음

요청 후 미조회·미제출 지인 비율이 높나

알림·리텐션 기획

8

평가 항목 확장

태도 항목 고정(§2-2), 사용자 커스텀 없음

항목 추가·커스텀 요구가 있나

없음

9

반언어 통계와 역할 중복 정리

반언어 통계(말속도·군말·침묵) 측정 자체가 MVP 제외

반언어 통계가 도입되나

STT 모델 확정 + 반언어 통계 도입(Part 2/3). 도입 시 태도 항목(특히 목소리 크기)과 역할을 다시 가른다

10

지인 신원 신뢰도

별칭만 받고 작성자를 검증하지 않는다

누가 썼는지의 신뢰가 문제가 되나

최소 확인 절차(#3 간이 인증과 함께)

11

레포트/리포트 표기

Part 4 내부는 "레포트"로 통일, 문서군 표준은 미정

문서군 표기를 언제 통일하나

Part 3("리포트")와 표기 협의

7. 디자인 요청 사항 (2026-07-11 시안 리뷰 반영)

큰 틀은 거의 그대로 가고, 지인 피드백 부분만 한 군데 구조를 바꾸면 될 것 같아서 정리해 드려요.

지금 피그마 파일을 열어두고 아래 순서대로 보시면 어디를 손대면 되는지 바로 보일 거예요.

그대로 가도 되는 것 (안 건드려도 돼요)

상세 레포트 아래에 지인 피드백 섹션이 이어지는 전체 흐름, 질문 분석 카드, 그리고 "태도분석 / 하고싶은 말" 탭 2개 구성 - 이건 기획 의도랑 정확히 맞아요. 지금 시안 그대로 두시면 됩니다.

탭 이름도 좋아요. "태도분석 / 하고싶은 말"이라는 표시 이름은 디자인에서 확정해 주시면 되고, 기획 문서에서는 편의상 "태도분석 / 기타"라는 내부 이름으로 부를게요. 화면에 나가는 건 디자이너가 정한 이름이에요.

바꿔야 할 것 한 가지 → 지인 피드백을 "합친 값"에서 "지인별 카드"로

지금 시안에서 어떻게 생겼나

지금은 항목(시선·표정 등) 하나에 대해 척도값 하나 + "지인 2명" 칩 + 코멘트 한 줄, 이렇게 하나로 합쳐서 보여주고 있어요. 지인이 딱 1명이면 이 형태로 아무 문제가 없는데, 2명 이상이 되면 세 가지가 표현이 안 돼요.

왜 합친 형태로는 안 되나 (지인이 여러 명일 때)

평가가 서로 다를 때가 문제예요.

예를 들어 "시선"을 민지님은 "잘 맞춰요"로, 다른 지인은 "가끔 피해요"로 골랐다고 해볼게요.

값을 하나만 보여주려면 둘 중 한 명의 평가를 버리거나 다수결로 합쳐야 하는데, "사람마다 다르게 본 것 자체가 사용자한테 중요한 정보"이지 않을까 생각을 해봤어요.

고민해본 결과 합치지 않기로 했어요. 서로 다른 두 평가를 나란히 보여주고 싶은 거예요.

코멘트도 누구 말인지가 사라져요.

항목당 코멘트가 한 줄이면 두 지인이 각자 남긴 코멘트 중 하나만 남고, 그게 누가 쓴 건지도 알 수 없어요.

별칭이 들어갈 자리가 없어요.

지인은 평가를 시작하기 전에 자기 별칭(예: "민지")을 입력하는데, 이게 최종 레포트에서 "이 평가는 누가 한 거야"를 보여주는 이름표예요.

지금처럼 합쳐 놓으면 이 이름표를 붙일 데가 없어요.

그래서 이렇게 바꿔주시면 돼요

항목 제목(예: "시선") 아래에, 지인 한 명당 카드 하나씩을 세로로 쌓아 주세요.

카드 한 장 안에는 세 가지가 들어가요:

별칭 이름표(예: "민지")

4단계 척도값(예: "가끔 피해요")

코멘트.

코멘트는 지인이 안 썼으면 빼고 이름표랑 척도값만 보여주면 돼요.

카드가 끝없이 늘어날 걱정은 안 하셔도 돼요.

한 면접에 지인은 최대 4명까지라, 한 항목 아래 카드도 최대 4장이에요.

카드 순서는 제출한 순서를 생각하고 있는데, 더 자연스러운 정렬이 있으면 그쪽으로 가셔도 돼요.

코멘트가 길 때 접기/더보기로 처리할지도 디자이너가 정해 주세요.

상황별로 뭘 보여주고 뭘 숨길지는 §2-4 표에 다 적어뒀는데, 핵심만 말하면 이래요:

아무 지인도 평가 안 한 항목은 그 항목 자체를 아예 안 보여줘요(빈 칸으로 남기지 않아요).

일부 지인만 평가한 항목은 평가한 사람 카드만 보여주고요("2명 중 1명" 같은 표시는 안 해요).

참여한 지인 수를 보여주고 싶으면, 지금처럼 항목마다 "지인 2명" 칩을 붙이는 대신 섹션 맨 위에 딱 한 번, "지금까지 2명이 참여했어요" 정도로 부드럽게 써주세요. → 제외도 가능합니당

지인은 제품에 로그인을 안 해서 인원수가 정확한 숫자가 아니라 대략치거든요.

그래서 "2명"이라고 딱 잘라 말하기보다 참고용 느낌으로 두는 게 좋아요(§2-5).

이참에 같이 정하면 좋을 것 3가지

항목별 척도 문구예요.

항목마다 4단계 표현이 필요한데, 지금은 "시선"만 정해졌어요

(잘 맞춰요 / 대체로 맞춰요 / 가끔 피해요 / 자주 피해요, 왼쪽이 좋은 쪽).

나머지 표정·자세·손 움직임·목소리 크기 4개도 각각 4단계 문구를 잡아야 하고, 바텀시트 맨 위에 뜨는 질문형 헤드라인(예: "OO님은 눈을 잘 마주치나요?")도 항목별로 세트가 필요해요.

지금 시안에서 자세·손 움직임·목소리 크기에 "밝아요"가 똑같이 들어가 있는 건 placeholder로 이해했어요.

이건 같이 앉아서 정하면 좋겠어요.

게스트 첫 화면(G4 온보딩)에 별칭 입력칸을 하나 넣어주세요.

위에서 말한 카드 이름표가 되는 값이에요.

필수는 아니고, 비워두면 "지인1"처럼 자동으로 붙는다는 안내 문구도 같이 넣어주시면 돼요.

맨 위 헤드라인 카피는 저희가 기준을 하나 더 드릴게요.

지금 시안의 "안정감 있게 마무리했어요!"는 placeholder인 거 알지만, 실제로 나갈 카피에는 "자신감·긴장·안정감" 같은 인상을 넘겨짚는 표현을 안 쓰기로 한 규칙이 있어요(Part 3 §2-2).

관찰된 사실만 쓰는 방향이라, 쓰면 안 되는 예시 목록을 따로 정리해서 드릴게요.

궁금하거나 더 나은 방법이 있으면 편하게 말씀 주세요. 카드 안을 어떻게 그리느냐는 얼마든지 열려 있어요.

